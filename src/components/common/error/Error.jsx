import React from 'react';
import { Alert } from 'reactstrap';

const Error = () => (
  <div className="error">
    <Alert color="danger">
      Oops, something went wrong!
    </Alert>
  </div>
);

export default Error;
